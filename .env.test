# Test environment configuration
# This file is used during testing to ensure consistent environment variables

# MCP Server Configuration
TRANSPORT=sse
HOST=0.0.0.0
PORT=8051

# PostgreSQL Test Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=test_db
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password

# OpenAI API Key for testing (mock key)
OPENAI_API_KEY=test_api_key

# Optional: Model choice for testing
MODEL_CHOICE=
